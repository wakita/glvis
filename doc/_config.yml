title: glvis project
email: wakita@is.titech.ac.jp
description: glvis project
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  wakita

# Build settings
destination: /Users/wakita/Sites/glvis

exclude:
  - Gemfile
  - Gemfile.lock
  - bin

theme: minima

markdown: Pandoc

pandoc:
  extensions:
    - mathjax
    - number-sections
    - standalone
    - table-of-contents
    - toc-depth=2

gems:
    - jekyll-feed
    - jekyll-pandoc
