# vertex shader
# version 410

in vec4 position_vs;

void main() {
  gl_Position = vec4(vec3(position_vs), 1);
  gl_PointSize = position_vs.w;
}

# fragment shader
# version 410

out vec4 color;

void main() {
  vec2 r = gl_PointCoord.xy * 2.0 - 1.0;
  if (dot(r, r) > 1.0) discard;
  color = vec4(0, 0, 1, 1);
}
